{%- if userComputed.layout.footer.show %}
    <div class="footer">
    <hr>


     {%- if userComputed.layout.footer.updated %}
        {%- set updated_different = userComputed.created.toISOString().slice(0, 10) != userComputed.updated.toISOString().slice(0, 10) %}
        {%- if updated_different %}
            <p class="updated-date">
                Edited:
                <time datetime="{{ userComputed.updated | dateToRfc3339}}"> {{ userComputed.updated |toDateHuge}}</time>        
                
            </p>
        {%- endif %}
    {%- endif %}
        
        {%- if false and userComputed.layout.footer.builtat %}
            <p class="builtat">
                <a href="/">philipp flenker's page</a> was last updated 
                <time datetime="{{ userComputed.builtAt | dateToRfc3339}}"> {{ userComputed.builtAt |toDateMedWithWeekday}}</time>
                .
            </p>
        {%- endif %}
        <p class="footer-invader">
        👾
        </p>
    </div>
{%- endif %}